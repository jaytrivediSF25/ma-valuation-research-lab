# Assumptions Policy

- Every non observed input is explicit in config
- Sector pack overrides must be traceable in outputs
- Sensitivity ranges must bracket base assumptions and be documented in output tables
